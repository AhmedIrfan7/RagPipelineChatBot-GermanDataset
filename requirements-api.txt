# Runtime-only dependencies for the API image. The server reads pre-extracted JSON,
# so the PDF-parsing stack (pdfplumber etc. in requirements.txt) is NOT needed here —
# keeps the deployed image small.
fastapi==0.139.0
uvicorn==0.51.0
pydantic==2.13.4
starlette==1.3.1
openai==2.45.0
python-dotenv==1.2.2
httpx==0.28.1
